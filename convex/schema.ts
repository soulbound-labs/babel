import { defineSchema } from 'convex/server';

/**
 * Empty schema (C6). Convex is present as scaffold only in Unit 01 — no tables,
 * no functions, no auth. The first real tables arrive with their first use
 * (Convex Auth + Presence in Unit 05·B / multiplayer).
 */
export default defineSchema({});
