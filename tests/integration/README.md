# Integration tests

**Post-MVP — intentionally empty in Unit 01.**

This directory is forward-declared so the convention exists before it is needed.
Integration specs (multiple modules wired together, e.g. an adapter against a
real port implementation) land here once Units 02–07 have real subsystems to
integrate. They are not part of the MVP unit-only test gate (C5).
