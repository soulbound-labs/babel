node_modules/
dist/
coverage/
convex/_generated/
pnpm-lock.yaml
*.txt

# Vendored substrate-kernel artifacts — keep their upstream formatting; not ours
# to reflow (avoids churn every time the kernel is updated).
docs/protocol/
docs/doctrine/agents-doctrine.md
docs/doctrine/agents-parallel-execution-doctrine.md
substrate.yaml

# tbd-managed generated content — regenerated by `tbd setup`, not ours to
# reflow. `.tbd/` is git-ignored (prettier doesn't read .gitignore, so it's
# listed explicitly); the skill installs are tracked but equally tbd-owned.
.tbd/
.claude/skills/tbd/
.agents/skills/tbd/
